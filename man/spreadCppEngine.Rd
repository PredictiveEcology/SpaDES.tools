% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{spreadCppEngine}
\alias{spreadCppEngine}
\title{Stochastic outward spread, in C++}
\usage{
spreadCppEngine(
  numCol,
  numCell,
  directions,
  loci,
  spreadProb,
  maxSize,
  minSize,
  iterations,
  jumpTries,
  jumpMeanDist,
  minSizeTries
)
}
\arguments{
\item{numCol}{Integer; number of raster columns.}

\item{numCell}{Integer; total number of raster cells.}

\item{directions}{Integer; 4 or 8.}

\item{loci}{Integer vector of starting cells, one per fire.}

\item{spreadProb}{Numeric, length 1 or \code{numCell}; probability of being spread to.}

\item{maxSize}{Numeric, length 1 or \code{length(loci)}; maximum cells per fire.}

\item{minSize}{Numeric, length 1 or \code{length(loci)}; cells each fire reaches
with persistence before the normal rule applies (see rule 6). 0 = none.}

\item{iterations}{Integer; maximum number of generations.}

\item{jumpTries}{Integer; jump attempts for a fire stuck under minSize (rule 7). 0 = none.}

\item{jumpMeanDist}{Numeric; mean jump distance in cells (rule 7).}

\item{minSizeTries}{Integer; rejections per fire before falling back to persistence (rule 8). 0 = none.}
}
\value{
A list of three integer vectors: \code{id}, \code{initialLocus}, \code{indices}; and, per
fire, \code{tries} (rejected attempts, rule 8) and \code{fallback} (1 if it fell back to rules 6-7).
}
\description{
Internal engine for \code{\link[=spreadCpp]{spreadCpp()}}. See the comments in \code{src/spread_cpp.cpp}
for the rules it implements and how they differ from \code{\link[=spread]{spread()}}.
}
\keyword{internal}
