% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{allInRange01}
\alias{allInRange01}
\title{Are all values in \verb{[0, 1]}, ignoring \code{NA}?}
\usage{
allInRange01(x)
}
\arguments{
\item{x}{Numeric vector.}
}
\value{
A single logical.
}
\description{
The question \code{spread()} asks of \code{spreadProb} before it starts. Expressed in R
as \code{all(inRange(na.omit(x)))} it costs three passes and two copies of the
whole vector -- \code{na.omit()} allocates one, the comparison another -- which is
work proportional to the size of the landscape on every call, however little
of it burns. This is one pass, no allocation, and it stops at the first value
that fails.

\code{NA} and \code{NaN} are ignored, so an all-\code{NA} vector and a zero-length vector are
both \code{TRUE}: there is nothing in them that is out of range. That matches
\code{all(inRange(na.omit(x)))}, which is \code{all(logical(0))}.
}
